@model List<Order>

<h1>Order List</h1>

<ul class="list-group">
    @foreach (var i in Model)
    {
        <li class="list-group-item">#@i.Id - @i.AnimalName made by ... at @i.Date.ToString()</li>
    }
</ul>